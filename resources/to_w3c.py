from resources.w3c_schema import text

def to_w3c(target_id:str,
           quote:str,
           prefix:str,
           suffix:str,
           start:str,
           end:str,
           text:str,
           user:str="blue"
           ) -> dict:
    ann = text()
    pass
    return {}
